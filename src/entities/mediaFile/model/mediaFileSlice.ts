import { createSlice } from "@reduxjs/toolkit";

export const mediaFileSlice = createSlice({
  name: "mediaFile",
  initialState: {},
  reducers: {},
  extraReducers: (builder) => {
    builder;
  },
});
